# Copy to terraform.tfvars and fill in — terraform.tfvars is git-ignored
aws_region      = "us-east-1"
environment     = "dev"
project         = "econ-pipeline"
lambda_zip_path = "../../../../lambda_collectors.zip"
